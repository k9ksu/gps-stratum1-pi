# Serve NTP to your local network from this GPS/PPS stratum-1 source.
#
# EDIT this to match YOUR network. Add one `allow` line per subnet you want to
# serve (CIDR notation). Leave it commented out to keep the server local-only.
allow 192.168.50.0/24
